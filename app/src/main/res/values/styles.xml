<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="MarbleStyle">
        <item name="android:textSize">@dimen/text_xlarge</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="MarbleStyle.Blue">
        <item name="android:background">@drawable/rounded_blue</item>
    </style>

    <style name="MarbleStyle.Green">
        <item name="android:background">@drawable/rounded_green</item>
    </style>

    <style name="MarbleStyle.Purple">
        <item name="android:background">@drawable/rounded_purple</item>
    </style>

    <style name="LineStyle">
        <item name="android:layout_height">@dimen/one_dp</item>
        <item name="android:background">@android:color/black</item>
    </style>

</resources>
